export enum ROUTES {
  ROOT = '/',
  AUTH = '/authorization',
  CREATE = "/create",
  PRODUCT = "/product",
  CATALOG = "/catalog",
  PROFILE = "/profile",
  EDIT = "/edit",
  RECOVERY = "/recovery",
  ERROR ="*"
}
